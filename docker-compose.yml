version: "3.9"

services:
  tree-service:
    build: .
    container_name: tree-service
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    volumes:
      - tree-data:/data
    restart: unless-stopped

volumes:
  tree-data:
